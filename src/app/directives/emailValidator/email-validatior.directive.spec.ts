import { EmailValidatiorDirective } from './email-validatior.directive';

describe('EmailValidatiorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailValidatiorDirective();
    expect(directive).toBeTruthy();
  });
});
